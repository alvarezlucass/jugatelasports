import dotenv from 'dotenv'; dotenv.config(); import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const { data } = await supabase.from('matches').select('id, start_time, status, home_score, away_score').in('id', ['1539001', '1489372']); console.log(data); } test();
