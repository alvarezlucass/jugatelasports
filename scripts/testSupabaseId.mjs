import { createClient } from '@supabase/supabase-js';

const url = process.env.VITE_SUPABASE_URL || 'https://xxx.supabase.co'; // wait I don't have it here. I can just read it from .env or just console log from the app.
